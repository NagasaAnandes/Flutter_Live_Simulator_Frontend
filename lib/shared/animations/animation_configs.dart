/// Shared Animations
///
/// Responsibility:
/// - Define reusable animation configurations
/// - Provide animation constants
/// - Support flutter_animate integration

/// Animation duration constants
class AnimationDurations {
  static const Duration fast = Duration(milliseconds: 150);
  static const Duration normal = Duration(milliseconds: 300);
  static const Duration slow = Duration(milliseconds: 500);
  static const Duration verySlow = Duration(milliseconds: 1000);
}

/// Animation curves
class AnimationCurves {
  // Use Flutter's standard curves
  // Custom curves can be added here
}
